# Python deps for the idle-watcher Cloud Function.
# Cloud Functions installs these during the build step; runtime is python312.
# Pin upper bounds to avoid breaking changes; lower bounds are the
# versions tested locally.

functions-framework>=3.0,<4
google-cloud-compute>=1.19,<2
google-cloud-storage>=2.16,<3
python-a2s>=1.4,<2
