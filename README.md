#### Setup

install `python3` and `pip3`
install requirements by using `requirements.txt`
have a `secrets.yaml` file in the root directory structured as follows:

```yaml
discord:
  token: <token-here>
```

#### Starting the Bot

Run `python3 bot.py` in your terminal of choice
